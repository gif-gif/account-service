# Account Service Web

The frontend lives in `web/` and is deployed as static assets independently from the backend.

## Environment

- `VITE_API_BASE_URL`: HTTPS origin for the backend API service.

## Commands

```bash
cd web
npm install
npm test -- --run
npm run build
npm run dev
```
